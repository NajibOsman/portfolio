<%= form_for([@post, @post.comments.build]) do |f| %>


  <div class="field">
    <%= f.label :comment%>
    <%= f.text_field :comment, id: :post_comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
